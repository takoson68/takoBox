Models manage application data, state, and business logic.

Put domain-specific models here, e.g. `CounterModel.js`.

Suggested patterns:
- No direct DOM access in Models.
- Expose methods/events for Controllers to consume.
